# Rust WA Module - Word Counter example

* Rust WA module example.

* Simple `word counter` written in Rust. Run natively via `cargo`.

* Build a simple WA Module. Run via `wasmtime`, or, via `wasmer`.
